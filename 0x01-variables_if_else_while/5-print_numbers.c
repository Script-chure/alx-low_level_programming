#include <stdio.h>

/**
 * main - Prints the numbers less than 10, from 0.
 *
 * Return: always 0(success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
